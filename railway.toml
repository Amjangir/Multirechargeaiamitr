# Root railway.toml — tells Railway this is a monorepo.
# Each service has its own railway.toml in its subdirectory.
# In the Railway dashboard, create two services and set their Root Directory:
#   Service 1 (backend)  → Root Directory = backend
#   Service 2 (frontend) → Root Directory = frontend
